extends ../layout/common.pug

block content
    h1 Login
    if errors
        each error in errors
            .alert.alert-danger=error
    form.form-inline.my-2.my-lg-0(method="POST")
        label email
        input.form-control.mr-sm-2(type='text', placeholder='nhập email',name='email',value=values ? values.email : "")
        brhon
        label password
        input.form-control.mr-sm-2(type='text', placeholder='nhập passwords ',name='password',value=values ? value.password:"")
        button.btn.btn-outline-success.my-2.my-sm-0(type='submit') Login
